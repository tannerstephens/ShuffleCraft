{% extends "base.jinja" %}

{% block content %}
  <div class="columns is-centered">
    <div class="column is-three-quarters">
      <div class="card">
        <div class="card-content">
          <p>ShuffleCraft is an unofficial Minecraft datapack generator that shuffles your recipes! The recipes for planks, crafting tables, and furnaces are unchanged,
          but the rest is up to you to figure out! Every pack is different, so give it a try!</p>
          &nbsp;
          <div class="columns">
            <div class="column is-one-quarter">
              <div class="select is-fullwidth is-large is-dark">
                <select id="version">
                {% for version in versions %}
                  <option>{{ version }}</option>
                {% endfor %}
                </select>
              </div>
            </div>
            <div class="column is-three-quarters">
              <a class="button is-large is-fullwidth is-dark" href="/download" id="download">Download Now</a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  <div class="columns is-centered">
    <div class="column is-three-quarters">
      <div class="card">
        <div class="card-content">
          <h1 class="title">Pack Installation</h1>
          <ol>
            <li>Open Minecraft</li>
            <li>Select the world you'd like to use the pack with</li>
            <li>Click Edit>Open World Folder</li>
            <li>Place the downloaded file in the datapacks folder</li>
            <li>Enjoy your shuffled crafting!</li>
          </ol>
        </div>
      </div>
    </div>
  </div>

  <script>
    $('#download').click(function(e) {
      version = $("#version").val()
      e.preventDefault();
      window.location.href = '/download?r=' + Math.random() + "&r2=" + Math.random() + "&v=" + version;
    })
  </script>
{% endblock content %}